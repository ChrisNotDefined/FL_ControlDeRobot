import 'package:clase02/src/pages/directions_page.dart';

import 'pages/directions_page.dart';
import 'package:flutter/material.dart';

class MyApp extends StatelessWidget {
  Widget build(context) {
    return MaterialApp(
      home: Center(
        child: DirectionsPage(),
      ),
    );
  }
}